if (WIN32)
  define_module(sg_gui OBJECT LINKS util cohear opengl glew boost INCLUDES ${CMAKE_CURRENT_SOURCE_DIR}/..)
else()
  define_module(sg_gui OBJECT LINKS util cohear x11 opengl glew boost INCLUDES ${CMAKE_CURRENT_SOURCE_DIR}/..)
endif()
